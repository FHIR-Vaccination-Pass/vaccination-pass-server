name: CI [develop]

on:
  push:
    branches:
      - develop
      # TODO: remove this before mergin
      - feat/21/docker-image

jobs:
  test:
    uses: ./.github/workflows/test.yml
  docker:
    uses: ./.github/workflows/docker.yml
    with:
      image: ghcr.io/fhir-vaccination-pass/vaccination-pass-server
